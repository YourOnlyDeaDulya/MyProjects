Электронная таблица наподобие Microsoft Excel

Динамическая таблица, которая позволяет хранить ячейки двух видов (Текстовые и Формульные). Есть возможность вывода таблицы на экран 
либо с сырым текстом внутри ячеек, либо непосредственно с вычислениями внутри них. Текстовые ячейки в обоих вариантах вернут текст.
Формульные ячейки позволяют ссылкаться на другие ячейки, при этом реализована проверка на цикличные зависимости. Внутри ячеек реализована
кэш-память, а так же оптимизированная инвалидация кэша ячейки и зависимых от нее.

Это комплексная работа, которую я использовал для тренировки архитектуры приложений. Работы с графом и алгоритмами. Помимо с++17 и CMake,
для парсинга строк в формулы использовалась сторонняя библиотека ANTLR. Без нее таблица не будет правильно работать.

В планах так же доработать интерфейс таблицы для более нативной работы (Работа непосредственно с таблицей и ячейками на экране)
